name: Manually deploy 2
on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Which env to deploy'
        required: true
        default: 'qa'
      version:
        description: 'Version of artifact'
        required: true
        default: ''

  # workflow_run:
  #   workflows: ["CI build"]
  #   branches: [main]
  #   types:
  #     - completed
env:
  ENVIRONMENT: "qa"
jobs:
  deploy:
    name: Deploy env ${{ env.ENVIRONMENT }} branch ${{ github.ref }} version ${{ github.event.inputs.version }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 10

    - name: Debug variables
      uses: hmarr/debug-action@v2
    - run: echo "env = ${{ env.ENVIRONMENT }}
    - name: Set outputs
      id: vars
      run: echo "::set-output name=sha_short::$(git rev-parse --short HEAD)"

    - name: Check outputs
      run: echo ${{ steps.vars.outputs.sha_short }}

    - name: Deploy to dev
      run: |
          echo "Deploy to ${{ github.event.inputs.environment }} with artifact ${{ github.event.inputs.version }}!"
      if: ${{ github.event.inputs.environment == 'dev' }} || ${{ github.workflow.name == 'CI build' }}

    - name: Deploy to qa
      run: |
          echo "Deploy to ${{ github.event.inputs.environment }} with artifact ${{ github.event.inputs.version }}!"
      if: ${{ github.event.inputs.environment == 'qa' }}

    - name: Deploy to stg
      run: |
        echo "Deploy to ${{ github.event.inputs.environment }} with artifact ${{ github.event.inputs.version }}!"
      if: ${{ github.event.inputs.environment == 'stg' }} &&  contains('["kingthorin","noinarisak"]', github.actor)

    # - uses: notlmn/release-with-changelog@v3
    #   with:
    #     token: ${{ secrets.GITHUB_TOKEN }}
    #     exclude: '^Meta'
    #     commit-template: '- {title} ← {hash}'
    #     template: |
    #       ### Changelog

    #       {commits}

    #       {range}

    #       ❤
